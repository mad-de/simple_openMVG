import sys
# Definiere hier welche Strings vom GUI ausgelesen werden soll
allstrings = ['inputpath', 'image1', 'image2'];

# Variablen initialisieren
index_argv = 1;
index_allstrings = 0;
index_user_vars = 0;
user_vars = {allstrings[0]: 'ERROR'};

# Durchlauf um Strings aus allstrings auszulesen und in dictionary user_vars mit den keys aus sys.argv zu speichern
for num in sys.argv[1:]:
 index_allstrings = 0;
 for num2 in allstrings[0:]:
  if sys.argv[index_argv].find(allstrings[index_allstrings]):
   pass
  else:
   user_vars[allstrings[index_allstrings]] = sys.argv[index_argv];
   index_user_vars = index_user_vars+1;
  index_allstrings = index_allstrings+1;
 index_argv = index_argv+1;

# Arrays vorbereiten um Inhalte auszulesen
index_allstrings = 0;
for num in user_vars:
 user_vars[allstrings[index_allstrings]] = user_vars[allstrings[index_allstrings]].replace(allstrings[index_allstrings] + "=", "")
 print allstrings[index_allstrings], 'hat Wert:', user_vars[allstrings[index_allstrings]]
 index_allstrings = index_allstrings+1;
"""
print 'Arrayausgabe2:'

index_allstrings = 0;
for num in user_vars:
 user_vars[allstrings[index_allstrings]].replace(allstrings[index_allstrings], "")
 print user_vars[allstrings[index_allstrings]]
 index_allstrings = index_allstrings+1;

print 'Arrayausgabe1:'

   user_vars.append([index_user_vars, allstrings[index_allstrings], sys.argv[index_argv] ]);
nums = sys.argv.split(' ', 2);
for num in nums:
print '1'
user_path=sys.argv[1]
print 'Path:', user_path
print 'Testausgabe:'
print 'Arguments:', ' '.join(sys.argv[1:])


s = " ";
str1 = "path";
str2 = "bath";

lyrics = s.join(sys.argv[1:])
nums = lyrics.split(' ', 2)
for num in nums:
if str1.find(str2) == 1:
 print '1'
else:
 print '0'
"""
